binary_sensor:
  - platform: gpio
    pin:
      number: ${pin}
      mode:
        input: true
        pulldown: true
      inverted: false
    name: ${label_motion_sensor}
    device_class: motion
    icon: mdi:motion-sensor
    filters:
      - delayed_on: 100ms
      - delayed_off: 5000ms
